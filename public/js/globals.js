// Global variables are bad and I should feel bad, I know
// Take special care when changing the file structure in the ERB
// file. These global variables are shared amongst js files because
// I am a horrible human being :D
var globalWordList = [];
var wavesurfer;
var zoomLevel = 20;